replicaCount: 1

image:
  repository: 363147303567.dkr.ecr.us-east-1.amazonaws.com/automation-test-api
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "1.2.20210524155538"

service:
  type: ClusterIP
  port: 80
  annotations: {}
  labels: {}

healthProbes: false

ingress:
  enabled: false

resources:
 limits:
   cpu: 300m
   memory: 512Mi
 requests:
   cpu: 300m
   memory: 512Mi

nodeSelector: {}

tolerations: []

affinity: {}

# Enable autoscaling
autoscaling:
  enabled: false

